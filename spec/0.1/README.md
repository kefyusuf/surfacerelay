# SurfaceRelay Contract 0.1 — Draft

This directory is provisional implementation material, not a standards claim.

Schemas:

- `action-definition.schema.json`
- `runtime-binding.schema.json`
- `invocation.schema.json`
- `action-result.schema.json`

Run `python scripts/validate.py` to validate schemas and fixtures.
